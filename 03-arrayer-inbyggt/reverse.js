// Omvänd ordning, utan att ändra originalet.
// Array.prototype.reverse ändrar arrayen på plats. Det finns en nyare metod som inte gör det.
export function reverse(list) {
  throw new Error('Ta bort den här raden och implementera funktionen');
}
