// Returnera en NY array med elementen i omvänd ordning.
// Samma ackumulator som i Elixir: reverse(list, acc = []).
// Inte Array.prototype.reverse (den ändrar originalet) och inte toReversed.
export function reverse(list, acc = []) {
  throw new Error('Ta bort den här raden och implementera funktionen');
}
